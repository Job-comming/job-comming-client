import React, { FC } from 'react'
import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({})

const ProfileMain: FC = () => {
  const classes = useStyles()
  return <div>profile feed list</div>
}

export default ProfileMain
